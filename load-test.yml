config:
  target: "https://backend-production-7e5f.up.railway.app"
  phases:
    - duration: 60
      arrivalRate: 10
      name: "Warm up"
    - duration: 120
      arrivalRate: 50
      name: "Heavy load"

scenarios:
  - name: "Health check"
    flow:
      - get:
          url: "/health"